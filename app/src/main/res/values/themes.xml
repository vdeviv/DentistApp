<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema base de la app — usa el tema de Material sin ActionBar
         porque usamos Jetpack Compose para toda la UI -->
    <style name="Theme.MyDentApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
